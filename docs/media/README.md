# Media

Binary demo assets for `genesis-nav`. Currently a placeholder.

Expected files (see `docs/demo_script.md` for recording instructions):

- `smoke_demo.gif` — 12s README hero clip of `gnav run`, run artifacts,
  replay, and bench.
- `walkthrough.mp4` (or YouTube link) — the full 2-minute demo.

Keep this directory under ~10MB total. Larger captures should be hosted off
the repo and linked.
