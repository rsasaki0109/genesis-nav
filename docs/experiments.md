# Experiments

## Current Baseline

| Date | Scenario | Commit | Result | Notes |
|---|---|---|---|---|
| 2026-05-28 | `examples/scenarios/smoke.yaml` | pending | success_rate=1.0, time_to_goal≈5.3s, path≈2.14m, 265 sim steps | Single diff-drive agent navigates to `(2, 1, 0)` via in-memory kinematics fallback. |
| 2026-05-28 | `examples/scenarios/warehouse_10_agents.yaml` | pending | success_rate=1.0 over 3 tasks, time_to_goal_mean≈11.4s, 568 sim steps | 10 spawned diff-drive agents with 3 assigned tasks; no collision modelling yet. |
| 2026-05-28 | `examples/scenarios/smoke.yaml --ros` | pending | success_rate=1.0, time_to_goal≈5.3s, 265 sim steps, bridge publishes 6+N topics | ROS 2 bridge enabled; `/clock`, `/genesis_nav/events`, per-agent `/state` and `/odom`, tf, tf_static visible while events flow to JSONL and topic. |
| 2026-05-28 | `examples/scenarios/smoke.yaml --backend genesis` | pending | aborts with install hint when Genesis is missing | Confirms `gnav doctor` and the CLI surface report Genesis availability before runtime startup. Run on a host with Genesis to record the in-Genesis baseline. |
| 2026-05-28 | `examples/scenarios/warehouse_10_agents.yaml --fast` (post-dispatcher) | pending | success_rate=1.0 over 3 tasks via dispatcher, task_dispatched_count=3, task_pending_peak=3, 568 sim steps | All tasks flow through `submit_task → dispatcher → assign_task`; reservation counters stay 0 because the scenario declares no shared resources. |
| 2026-05-28 | `examples/scenarios/smoke.yaml --fast` (post-J: env+replay strict) | pending | env.json populated (python/platform/git/ros_distro/genesis_version), replay strict ok with `--print-events` streaming SCENARIO_STARTED→TASK_ASSIGNED→TASK_STARTED→TASK_SUCCEEDED→SCENARIO_FINISHED | Confirms Workstream J run-directory contract: env metadata captured, replay refuses missing artifacts / corrupt events / metrics without required keys. |
| 2026-05-28 | `gnav bench --run benchmarks/{nav_basic,multi_agent,runtime,humanoid}` | pending | 4 suites, 1 scenario each, all green: nav_basic 1/1 (sr=1.0, ttg≈5.3s), multi_agent 1/1 (4 succeeded), runtime 1/1 (sim_steps=265), humanoid 1/1 | First Workstream K harness pass. Reports under `benchmarks/_runs/<suite>_report.json` each list `run_dir` for failure debug. |
| 2026-05-28 | `examples/scenarios/smoke.yaml --fast` (post-H: navigation MVP) | pending | success_rate=1.0, BEHAVIOR_STATE_CHANGED stream idle→assigned→planning→executing→succeeded→idle, PLAN_RESOLVED carries planner+waypoint_count, stuck/recovery counters present in metrics.json | Confirms Workstream H wiring: `StraightLinePlanner` fallback, behavior state machine emitted alongside task lifecycle, no regressions in the 94 pre-H tests. |
| 2026-05-28 | Stuck-and-recover unit test (`tests/unit/test_navigation_mvp.py::test_stuck_detector_triggers_recovery_then_fails`) | pending | AGENT_STUCK fires once, behavior transitions executing→recovering→executing→failed→idle, TASK_FAILED reason="stuck" | First end-to-end exercise of the new recovery loop. `stuck_window_sec=0.5`, `max_recovery_retries=1`. |
| 2026-05-28 | Workstream N — docs and launch surface (no runtime change) | pending | CONTRIBUTING.md + 5 new issue templates + good-first-issues catalogue + comparison table + demo / launch scripts shipped; no rerun of smoke required | `gnav run` and `gnav bench` outputs unchanged; verified by inspection that no `genesis_nav/`, `ros2_ws/`, or `tests/` source files were touched. |
| 2026-05-29 | `make demo-gif` (asciinema + agg) | pending | `docs/media/smoke_demo.gif` 486KB / 979x649, cast 11KB; sequence: `gnav run` success_rate=1.0 → `report.md` head → `gnav replay --print-events` 5 events → `gnav bench` 1/1 passed | First fully scripted README hero capture; `make demo-gif` reproduces deterministically from `scripts/record_demo.sh`. |
| 2026-05-29 | v0.1.0 release tag | pending | All 3 CI workflows (`ci` / `docs` / `ros2`) green on `efde985`; pyproject bumped 0.1.0a0 → 0.1.0; CHANGELOG.md added | Closes PLAN.md §9 v0.1 milestone. Outstanding items rolled into v0.2 (see issue #10). |
| 2026-05-29 | `examples/scenarios/smoke.yaml --fast --backend ros2_robot` | pending | first v0.2 slice: `--backend ros2_robot` builds a real rclpy node, publishes `/<agent>/cmd_vel`, subscribes `/<agent>/odom`, exits 0; 116 unit tests pass (+7 ros2_robot, all rclpy-free via `FakeRobotTransport`) | Dry connection only — no odom source on the host, so poses stay at origin. Verifies the outbound hardware edge + `CommandGate` path; real-robot loop closure is future work. See the 2026-05-29 real-robot ADR. |
| 2026-05-29 | dynamic-obstacle replan (unit e2e, `test_dynamic_obstacles.py`) | pending | second v0.2 slice: obstacle dropped on a 6-wide grid corridor at `at_sec=0.1` → `OBSTACLE_CHANGED` recorded, agent re-enters `planning` (`REPLAN_TRIGGERED`), routes around `[3,1]`, reaches goal; success_rate=1.0, replan_count≥1; 123 unit tests pass (+7) | Replan extends the planner contract (`OccupancyGrid.with_blocked`, `executing→planning` edge), not a new subsystem. Deltas are events so replays reconstruct the timeline. See the 2026-05-29 dynamic-obstacles ADR. |
| 2026-05-29 | Nav2 planner backend (unit, `test_nav2_planner.py`) | pending | third v0.2 slice: `runtime.navigation.planner: auto\|grid\|straight\|nav2` selector + `Nav2Planner` delegating to a `Nav2PathService`; selection + delegation verified with `FakeNav2PathService` (rclpy/nav2_msgs mocked out); 133 unit tests pass (+10) | Nav2 stays a planner backend behind `plan()`, not a runtime replacement. Generalizes issue #9's selector. Global-planning delegation only; controller `cmd_vel`-through-`CommandGate` + `env.json` Nav2 version capture are follow-ups. See the 2026-05-29 Nav2 ADR. |
| 2026-05-29 | teleop operator override (unit, `test_teleop.py`) | pending | fourth v0.2 slice: `Runtime.submit_teleop_command` runs a TELEOP command through `CommandGate`, emits COMMAND_ACCEPTED/REJECTED, and holds off autonomy for `teleop_hold_sec`; verified teleop overrides autonomy for the hold window then autonomy resumes; 138 unit tests pass (+5) | ROS-free operator path sharing `CommandGate`+`apply_external_command` with the bridge. sim-time hold → replayable. Unifying the bridge onto this API is a follow-up. See the 2026-05-29 teleop ADR. |
| 2026-05-29 | hardware diagnostics (unit, `test_diagnostics.py`) | pending | fifth v0.2 slice: `collect_diagnostics` folds estop/fall/failed/watchdog into per-agent OK\|WARN\|ERROR; `Runtime.diagnostics()` + `AgentToolApi.get_diagnostics()` read-models; periodic `DIAGNOSTICS` event when `diagnostics_interval_sec>0` (verified emitted; off by default); 147 unit tests pass (+9) | Gives the real-robot watchdog a consumer. Pure duck-typed collector — new health axes (battery, temp, comms) raise a level in one function. See the 2026-05-29 diagnostics ADR. |
| 2026-05-29 | v0.2.0a0 alpha tag | pending | session review + 動作確認 all green: 148 unit tests pass, all 5 slices verified e2e (replay OK, graceful nav2-no-server, teleop override, diagnostics levels), CI (`ci`/`docs`/`ros2`) green; pyproject + `__init__` bumped 0.1.0 → 0.2.0a0 | Marks the v0.2 groundwork milestone. Alpha: real-robot loop, Nav2 controller delegation, watchdog auto-poll remain follow-ups. Fixed one review finding (nav2 CLI unavailability now exits 4, not a traceback). |
| 2026-05-29 | `benchmarks/nav_basic` (post-tag: + `dynamic_obstacle_replan.yaml`) | pending | suite now 2/2 passed; new scenario guards the replan path with `obstacle_event_count_min: 1` + `replan_count_min: 1` (new bench predicate keys); `metrics.json` now carries `replan_count` / `obstacle_event_count`; 149 unit tests pass (+1) | Regression guard for the v0.2 dynamic-obstacle replan via the existing `gnav bench` harness. Required exposing the two counters in `MetricsSnapshot`. |
| 2026-05-29 | ROS bridge `/cmd_vel` unified onto `submit_teleop_command` (`test_ros_bridge.py` via colcon `genesis_nav_msgs` + rclpy) | pending | follow-up (a): `RosBridge._on_cmd_vel` is now pure transport, forwarding to `submit_teleop_command(requester_id="ros_cmd_vel")`; bridge no longer takes a `CommandGate`. Both rclpy bridge tests pass (2/2) against real rclpy + locally-built `genesis_nav_msgs`; 150 core unit tests pass (+1, rclpy-free) | Closes the bridge teleop gap: external `/cmd_vel` now carries `requester_id` (metadata rule) and sets the autonomy hold (operator over ROS truly overrides autonomy). Gate/events/apply/hold all live in the runtime. See the 2026-05-29 teleop ADR update. |
| 2026-05-29 | real-robot command-watchdog auto-poll (unit, `test_command_watchdog.py`) | pending | follow-up (c): `Runtime._poll_safety_signals` now auto-polls `Ros2RobotAdapter.watchdog_expired` on the transport's monotonic clock; a stalled command pipeline trips `SAFETY_STOP` (`reason="command_watchdog"`) once, zeroes the actuator, latches the emergency stop, and bumps `watchdog_stop_count`. Verified: sim runs never trip (duck-typed), stale command trips exactly once, fresh commands stay quiet; metrics.json carries `watchdog_stop_count`, replay strict OK, `gnav bench` nav_basic 2/2; 153 unit tests pass (+3) | Gives the real-robot watchdog a teeth-bearing consumer (diagnostics was read-only WARN). Latched + direct stop so an idle/teleop robot is zeroed too. New bench predicates `watchdog_stop_count_min`/`_max`. See the 2026-05-29 real-robot ADR update. |
| 2026-05-29 | env.json Nav2 version capture (unit, `test_run_artifacts.py`) | pending | follow-up (d): `collect_env_metadata` now records `nav2_version`, resolved from the Nav2 `package.xml` `<version>` via `ament_index_python` (tries `nav2_bringup`/`nav2_msgs`/`nav2_core`). Best-effort + never raises: core CI / pure-sim collect `""`; on this Jazzy host it resolved `1.3.11`. Parsing covered by `_read_package_xml_version` tests; 157 unit tests pass (+3) | Closes the `env.json` half of the Nav2 reproducibility follow-up (`ros_distro` already had the distro). A `nav2` replay now states which Nav2 it ran against. Marking nav2 scenarios integration-only remains open. See the 2026-05-29 Nav2 ADR update. |
| 2026-05-29 | nav2 benchmark scenarios marked integration-only (unit + e2e, `test_benchmark_harness.py`) | pending | follow-up (e): scenarios with `benchmark.integration: true` are skipped by `gnav bench --run` unless `--include-integration` is passed; skips are recorded under the report's `skipped` array + logged (no silent truncation). New `benchmarks/nav2_integration/single_agent_nav2.yaml` (`planner: nav2`). Verified e2e: default run skips (0/0 passed, 1 skipped, rc 0); `--include-integration` attempts it and fails without a live Nav2 server (rc 1); `nav_basic` still 2/2; 159 unit tests pass (+2) | Closes the benchmark half of the Nav2 reproducibility follow-up — deterministic suites stay green without an external stack, the integration path is one flag away. Routing Nav2 controller `cmd_vel` through `CommandGate` is the sole remaining Nav2 follow-up. See the 2026-05-29 Nav2 ADR update. |
| 2026-05-29 | Nav2 controller cmd_vel through CommandGate (unit + e2e, `test_nav2_controller.py`) | pending | follow-up (b): `runtime.navigation.controller: local\|nav2`; `Nav2Controller` is a drop-in for `SimpleLocalController` backed by the `Nav2ControllerService` boundary, so Nav2's velocity flows through the *existing* `CommandGate` autonomy path. Verified: Nav2-sourced velocity drives the agent to goal (success_rate=1.0) with `COMMAND_ACCEPTED` carrying `source=nav2_controller` at `authority=autonomy`; a non-finite Nav2 velocity is rejected by the gate (`COMMAND_REJECTED`, 0 accepts); falls back to in-tree controller when Nav2 has no command. e2e with real rclpy: `controller: nav2` builds a node + subscribes, degrades gracefully to local (source `navigation`) with no Nav2 server (rc 0); 168 unit tests pass (+9) | Closes the last Nav2 ADR follow-up — no new apply path, the AI safety boundary holds by construction. `COMMAND_ACCEPTED` now carries `source`. nav2_integration scenario extended to full delegation (planner + controller). See the 2026-05-29 Nav2 ADR update. |
| 2026-05-29 | real-robot loop closure via loopback transport (unit + e2e, `test_robot_loopback.py`) | pending | `real_robot.transport: loopback` selects a rclpy-free `LoopbackRobotTransport` that integrates the commanded velocity into odom (same diff-drive model as `DiffDriveKinematics`), closing the real-robot loop in process. The *same* `Ros2RobotAdapter` is used — only the transport differs. Verified e2e: `gnav run --backend ros2_robot` with `transport: loopback` reaches the smoke goal **without rclpy** (success_rate=1.0, 265 sim steps = fallback baseline, confirming the integration model agrees), replay strict OK; no spurious command_watchdog SAFETY_STOP while commands flow; 178 unit tests pass (+10) | Closes the "real-robot loop closure is future work" item from the first ros2_robot slice — the outbound edge was proven before, now the full CommandGate→apply_command→publish→odom→read_pose loop runs deterministically in core CI. Keeps the realistic one-tick odom lag. See the 2026-05-29 real-robot ADR update. |
| 2026-05-29 | inter-agent proximity detection (unit + e2e, `test_collision_detection.py`) | pending | `runtime.collision` block (`collision_radius_m`/`near_miss_radius_m`, default 0=off) + `_poll_collisions` emits `COLLISION`/`NEAR_MISS` + bumps `collision_count`/`near_miss_count` on the rising edge of a pair entering a radius. Observation only (no stop/reroute). Verified: disabled by default (existing scenarios unchanged), counted once per approach + re-armed on separation, near-miss vs collision split, receding-from-collision not double-counted; e2e warehouse_10 run registered 11 near-misses (sr=1.0 unchanged); new `benchmarks/multi_agent/near_miss_detection.yaml` (perpendicular crossing → near_miss=1, collision=0) passes, multi_agent suite now 2/2; replay shows NEAR_MISS in the timeline; 187 unit tests pass (+9) | First step against the v0.1 "no spatial conflict resolution" limitation — makes the dead `collision_count`/`near_miss_count` metrics + bench predicates real. Detection before control; proximity *response* (yield/replan) is the follow-up. New bench predicates `collision_count_min`/`near_miss_count_min`. See the 2026-05-29 proximity-detection ADR. |
| 2026-05-29 | proximity response / yield (unit + e2e, `test_proximity_response.py`) | pending | `runtime.collision.yield_radius_m` (0=off): an executing agent yields (stops this tick) while a higher-priority agent (lexicographic agent-id order, deadlock-free total order) with an active task is within the radius. Emits `AGENT_YIELDED` + `yield_count`; resets stuck window; never yields to an idle agent. Verified e2e on a shared-crossing-point geometry: detection-only → collision=1/near_miss=1; yield on → collision=0/near_miss=0/yield=6, both agents still reach goal (sr=1.0). New `benchmarks/multi_agent/yield_avoidance.yaml`; multi_agent suite 3/3; replay shows AGENT_YIELDED; 193 unit tests pass (+6) | Proximity *response* on top of the detection slice — resolves crossing conflicts (stop-and-wait). Head-on (lateral reroute), smarter priority, costmap reservation are follow-ups, now layerable on a tested detect→respond loop. New predicates `yield_count_min`/`yield_count_max`. See the 2026-05-29 proximity-detection ADR update. |
| 2026-05-29 | spatial safety folded into diagnostics (unit + e2e, `test_diagnostics.py` / `test_proximity_response.py`) | pending | `collect_diagnostics` now folds `state.in_collision` (→ ERROR) and `state.yielding` (→ WARN), maintained by the runtime each tick. Verified: pure-collector levels + collision-outranks-yield; live `diagnostics()` reports b WARN/yielding during the crossing; periodic `DIAGNOSTICS` event e2e shows 19 non-OK snapshots (b WARN/yielding) on the yield scenario; existing diagnostics tests unaffected (getattr defaults); 197 unit tests pass (+4) | Consolidation: the detect→near-miss→yield slices now have an operator/AI-facing health consumer + replayable timeline, the same pattern as the watchdog. Chosen over head-on lateral reroute (a larger local-planner effort, kept as the explicit follow-up). See the 2026-05-29 diagnostics ADR update. |

## Failed Experiments

No failed experiments recorded yet.

## Known Flaky Scenarios

No flaky scenarios recorded yet.

## Reproduction Commands

```bash
gnav run examples/scenarios/smoke.yaml --fast --record
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python3 -m pytest tests/unit

# ROS 2 bridge end-to-end
source /opt/ros/jazzy/setup.bash
colcon build --base-paths ros2_ws/src --packages-select genesis_nav_msgs genesis_nav_ros genesis_nav_bringup
source ros2_ws/install/setup.bash
gnav run examples/scenarios/smoke.yaml --fast --ros
```

## Benchmark History

Benchmark history starts once v0.1 scenario execution is wired to Genesis.
