# Bag Tools

Rosbag recording is implemented in `genesis_nav/ros/bag_writer.py` and wired
into `gnav run --record --ros`. Topic profiles live under `configs/rosbag/`.

Future utilities for profile selection, inspection, and replay validation may
also live here.
